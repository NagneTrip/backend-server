<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.nagne.repository.ArticleRepository">
    <insert id="save" parameterType="Article" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article(user_id,
                            content,
                            good,
                            created_date,
                            last_modified_date)
        VALUES (#{article.userId},
                #{article.content},
                0,
                now(),
                now())
    </insert>

    <select id="findById" resultType="Article">
        SELECT *
        FROM article
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="Article">
        SELECT *
        FROM article
        WHERE id &lt; #{pageParameter.lastIndex}
        ORDER BY id DESC LIMIT #{pageParameter.size}
    </select>

    <select id="findFollowingArticles" resultType="Article">
        SELECT *
        FROM article
        WHERE user_id in
              (SELECT following_id
               FROM follow
               WHERE follower_id = #{id})
          AND id &lt; #{pageParameter.lastIndex}
        ORDER BY id DESC LIMIT #{pageParameter.size}
    </select>

    <update id="update">
        UPDATE article
        SET
        <if test="article.content != null">
            content = #{article.content},
        </if>
        last_modified_date = now()
        WHERE id = #{id}
    </update>

    <update id="updateGoodCount">
        UPDATE article
        SET good = good + 1
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE
        FROM article
        WHERE id = #{id}
    </delete>
</mapper>